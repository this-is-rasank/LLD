Chain of Responsibility Principle is used when we don't want to define what child class to use.
Similar to Factory pattern but it creates all child classes and send request to next until someone serves it.
Used in ATM, Logger and Vending Machine